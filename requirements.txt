streamlit==1.61.1
# Streamlit already installs pandas; pinned here so the closed list below is
# true rather than nearly true. Charts want DataFrames.
pandas==3.0.5
numpy==2.4.6
matplotlib==3.11.1
python-dotenv==1.2.3
pytest==9.1.1
